import {IActionRequest, IRequest} from "../../interfaces/interfaces";

const requests = (state: IRequest = {}, action:IActionRequest) => {
    switch (action.type) {
        default:
            return state
    }
}

export default requests